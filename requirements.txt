# Dépendances Python pour les scripts du projet
# pip install -r requirements.txt
 
boto3>=1.34.0       # Client MinIO/S3
trino>=0.328.0      # Client Trino SQL
pandas>=2.0.0       # Lecture et manipulation des CSV
pyarrow>=14.0.0     # Écriture Parquet (requis par la nouvelle stratégie de chargement)
 
